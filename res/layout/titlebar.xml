<?xml version="1.0" encoding="utf-8"?>
<!-- 公用标题栏 -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/titlebar_style"
    android:background="@drawable/top_pink"
    >

	<!-- 左按钮 -->
    <ImageView
        android:id="@+id/iv_titlebar_goback"
        style="@style/titlebar_iv_style"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:background="@null" />
    <!-- 标题 -->

    <LinearLayout
        android:id="@+id/ll_titlebar_title"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_centerInParent="true"
        android:layout_centerVertical="true"
        android:clickable="true"
        android:gravity="center"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/tv_titlebar_title"
            style="@style/titlebar_text_style"
            android:textStyle="bold"
            android:textSize="16sp"
            android:layout_gravity="center_vertical" />

        <ImageView
            android:id="@+id/iv_titlebar_title_arrow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginLeft="5dp"
            android:background="@drawable/ic_arrow_down" />
    </LinearLayout>

    <!-- 搜索框 -->

    <LinearLayout
        android:id="@+id/ll_titlebar_searchbar"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_centerInParent="true"
        android:layout_centerVertical="true"
        android:gravity="center"
        android:visibility="gone" >

        <EditText
            android:id="@+id/et_titlebar_search"
            android:layout_width="200dp"
            android:layout_height="35dp" />
    </LinearLayout>

    <!-- 右按钮 -->
    <ImageView
        android:id="@+id/iv_titlebar_right"
        style="@style/titlebar_iv_style"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@null" />

</RelativeLayout>